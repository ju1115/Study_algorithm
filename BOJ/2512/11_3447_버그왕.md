# 📝 버그왕 (BOJ 3447)

### 💡 접근 방식 (Idea)
- input 을 받아 while 문으로 stirng처리

### ⚠️ 막혔던 부분 / 중요 포인트
- 이번 테케는 모두 통과했지만 stringtokenizer는 기존의 tab 스페이스 간격을 무시함 그냥 받아서 바로 append로 할것 ST에 너무 매몰되지 말자.

### 🗝️ 해결 코드 (Key Point)
```java
        String input;

        while ((input = br.readLine()) != null) {

            while (input.contains("BUG")) {
                input = input.replace("BUG", "");
            }

            sb.append(input).append("\n");
        }
```

### 📋 참고 자료
- 
