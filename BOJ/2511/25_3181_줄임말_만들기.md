# 📝 줄임말\_만들기 (BOJ 3181)

### 💡 접근 방식 (Idea)

- 스트링 토크나이저 들어오는 문자열이 set 에 있는 쓸모없는 문자와 동일한지 체크

### ⚠️ 막혔던 부분 / 중요 포인트

- 첫번째는 무조건 포함 조건을 잘 읽을 것.
- 단순 조회용 불변 객체 Set.of 로 생성가능 of는 ~으로 부터 구성됨 이라는 네이밍 룰.
- 기본 연산자가 속도가 더 빠르다. 또한 예외처리를 따로하는것이 비교연산을 수행하는것보다 이득.

### 🗝️ 해결 코드 (Key Point)

```java
    // 1. 첫 번째 단어는 무조건 포함 (Loop 밖으로 뺌 -> if문 제거 효과)
    if (st.hasMoreTokens()) {
        String firstWord = st.nextToken();
        // 전체 변환(toUpperCase) 대신 첫 글자만 추출해서 변환 (성능 Up)
        sb.append(Character.toUpperCase(firstWord.charAt(0)));
    }
    // 2. 나머지 단어들 처리
    while (st.hasMoreTokens()) {
        String word = st.nextToken();
        // uselessSet에 없을 때만 처리
        if (!uselessSet.contains(word)) {
            sb.append(Character.toUpperCase(word.charAt(0)));
        }
    }
```

### 📋 참고 자료

-
