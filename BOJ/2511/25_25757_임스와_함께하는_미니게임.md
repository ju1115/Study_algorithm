# 📝 임스와*함께하는*미니게임 (BOJ 25757)

### 💡 접근 방식 (Idea)

- N개의 요청을 모두 Set에 저장한다. Set에는 중복이아닌 유저만 남으므로 size / num

### ⚠️ 막혔던 부분 / 중요 포인트

- type 을 받을땐 valueOf를 활용
- 생성자로 상태를 주입할 수 있다.
- 초기 set 생성자 N을 주면 리사이징을 하지 않아 좋다.

### 🗝️ 해결 코드 (Key Point)

```java
    enum Type {
        Y(1), F(2), O(3);

        private final int score;

        Type(int score) {
            this.score = score;
        }

        public int getScore() {
            return score;
        }
    }
        Type type = Type.valueOf(st.nextToken());
        sb.append(userSet.size() / type.getScore());
```

### 📋 참고 자료

-
